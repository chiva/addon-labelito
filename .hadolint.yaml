# Home Assistant add-ons run as root under the Supervisor sandbox; labelito/Dockerfile
# deliberately ends on `USER root` so the mapped /data, /config, and USB device nodes
# (all root-owned) are accessible. Hadolint flags a final `USER root` as DL3002, but it is
# the required add-on convention here, so the rule is ignored intentionally.
ignored:
  - DL3002
